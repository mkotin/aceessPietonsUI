import { DemandeSearchPipe } from './demande-search.pipe';

describe('DemandeSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new DemandeSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
