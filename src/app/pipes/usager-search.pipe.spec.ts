import { UsagerSearchPipe } from './usager-search.pipe';

describe('UsagerSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new UsagerSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
